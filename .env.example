# Romela (Next.js)
# Rename this file to .env.example
# Copy it to .env.local for local development.
# NOTE: For static export (output: 'export'), env values are baked at build time.

# WordPress JSON base (includes /wp-json)
NEXT_PUBLIC_WP_JSON_BASE_URL=https://admin.padradarasoil.com/wp-json

# Backwards compatibility (optional)
# NEXT_PUBLIC_WORDPRESS_URL=https://admin.padradarasoil.com/wp-json
